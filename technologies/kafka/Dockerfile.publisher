# ZeroMQP2P Publisher Dockerfile
FROM kafka_base

# Set working directory
WORKDIR /app/build/core/applications

# Run the consumer app
ENTRYPOINT ["./PublisherApp"]
CMD [ "INFO", "DEBUG", "STUDY", "ERROR" ]
# Arrow Flight C++ Publisher Dockerfile
FROM arrowflight_bin_p2p_base

# Set working directory
WORKDIR /app/build/core/applications

# Run the publisher app
ENTRYPOINT ["./PublisherApp"]
CMD [ "INFO", "DEBUG", "STUDY", "ERROR" ]